// Auto-generated structured data
window.structuredData = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  "name": "Spokane Recovery Resources",
  "description": "Comprehensive directory of 0 recovery resources in Spokane, WA",
  "numberOfItems": 0,
  "itemListElement": []
};

// Inject structured data into page
document.addEventListener('DOMContentLoaded', function() {
    const script = document.createElement('script');
    script.type = 'application/ld+json';
    script.textContent = JSON.stringify(window.structuredData);
    document.head.appendChild(script);
});